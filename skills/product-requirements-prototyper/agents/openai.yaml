interface:
  display_name: "Product Requirements Prototyper"
  short_description: "Create and repair product delivery artifacts"
  default_prompt: "Use $product-requirements-prototyper to classify my request as full delivery, one stage, or screen-local refinement, then produce only the requested scope."
