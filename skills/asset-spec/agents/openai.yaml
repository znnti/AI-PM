interface:
  display_name: "Asset Spec"
  short_description: "Plan asset slot replacement rules"
  default_prompt: "Use $asset-spec to inventory asset slots, placeholder rules, and replacement prompts for one screen or asset scope."
